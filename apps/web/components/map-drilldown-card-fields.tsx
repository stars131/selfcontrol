"use client";

import type { MapDrilldownCardFieldsProps } from "./map-drilldown-card-fields.types";

export function MapDrilldownCardFields({ allLabel, allRecordsLabel, confirmedLabel, filterDraft, mapStatusLabel, mappedOnlyLabel, needsReviewLabel, onFilterDraftChange, pendingLabel, placeQueryLabel, placeQueryPlaceholder, reviewStatusLabel, unmappedOnlyLabel }: MapDrilldownCardFieldsProps) {
  return <div className="inline-fields"><label className="field"><span className="field-label">{placeQueryLabel}</span><input className="input" value={filterDraft.placeQuery} onChange={(event) => onFilterDraftChange((current) => ({ ...current, placeQuery: event.target.value }))} placeholder={placeQueryPlaceholder} /></label><label className="field"><span className="field-label">{mapStatusLabel}</span><select className="input" value={filterDraft.mappedOnly} onChange={(event) => onFilterDraftChange((current) => ({ ...current, mappedOnly: event.target.value as MapDrilldownCardFieldsProps["filterDraft"]["mappedOnly"] }))}><option value="all">{allRecordsLabel}</option><option value="mapped">{mappedOnlyLabel}</option><option value="unmapped">{unmappedOnlyLabel}</option></select></label><label className="field"><span className="field-label">{reviewStatusLabel}</span><select className="input" value={filterDraft.reviewStatus} onChange={(event) => onFilterDraftChange((current) => ({ ...current, reviewStatus: event.target.value as MapDrilldownCardFieldsProps["filterDraft"]["reviewStatus"] }))}><option value="all">{allLabel}</option><option value="pending">{pendingLabel}</option><option value="confirmed">{confirmedLabel}</option><option value="needs_review">{needsReviewLabel}</option></select></label></div>;
}
