import type {
  BuildRecordEditorWorkspaceBasePropsInput,
  RecordEditorWorkspaceBaseProps,
} from "./record-panel-v2-editor-workspace-base-props.types";

export function buildRecordEditorWorkspaceBaseProps({
  authToken,
  bulkRetryingDeadLetter,
  canWriteWorkspace,
  deleting,
  deletingMediaId,
  downloadingMediaId,
  error,
  form,
  formatFileCountLabel,
  formatHistoryTimestampLabel,
  formatReminderEnabledLabel,
  formatReminderStatusLabel,
  formatReminderTimestampLabel,
  formatReviewStatusLabel,
  locale,
  locationReviewForm,
  mediaAssets,
  mediaDeadLetterOverview,
  mediaIssueCopy,
  mediaProcessingOverview,
  mediaStorageSummary,
  panelCopy,
  refreshingMediaId,
  reminderForm,
  reminders,
  retryingMediaId,
  saving,
  savingReminder,
  selectedDeadLetterIds,
  selectedLocationHistory,
  selectedLocationReview,
  selectedRecord,
  selectedRecordMediaSizeLabel,
  setForm,
  setLocationReviewForm,
  setReminderForm,
  summarizeHistoryActionLabel,
  uploading,
  workspaceId,
}: BuildRecordEditorWorkspaceBasePropsInput): RecordEditorWorkspaceBaseProps {
  return {
    authToken,
    bulkRetryingDeadLetter,
    canWriteWorkspace,
    deleting,
    deletingMediaId,
    downloadingMediaId,
    error,
    form,
    formatFileCountLabel,
    formatHistoryTimestampLabel,
    formatReminderEnabledLabel,
    formatReminderStatusLabel,
    formatReminderTimestampLabel,
    formatReviewStatusLabel,
    locale,
    locationReviewForm,
    mediaAssets,
    mediaDeadLetterOverview,
    mediaIssueCopy,
    mediaProcessingOverview,
    mediaStorageSummary,
    panelCopy,
    refreshingMediaId,
    reminderForm,
    reminders,
    retryingMediaId,
    saving,
    savingReminder,
    selectedDeadLetterIds,
    selectedLocationHistory,
    selectedLocationReview,
    selectedRecord,
    selectedRecordMediaSizeLabel,
    setForm,
    setLocationReviewForm,
    setReminderForm,
    summarizeHistoryActionLabel,
    uploading,
    workspaceId,
  };
}
